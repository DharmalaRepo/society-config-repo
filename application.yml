spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DharmalaRepo/society-config-repo
          default-label: main
          search-paths: '{application}'
      label: main